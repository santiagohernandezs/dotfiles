# Template for local environment secrets
# Copy this file to '.zsh_secrets' (in the same directory) and fill in your keys.
# Note: '.zsh_secrets' is ignored by git so your secrets will never be committed.

export GEMINI_API_KEY="your_gemini_api_key_here"
export BWS_ACCESS_TOKEN="your_bitwarden_secrets_manager_token_here"
